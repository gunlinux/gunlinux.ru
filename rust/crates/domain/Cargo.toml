[package]
name = "domain"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
bcrypt = "0.15"
chrono = { version = "0.4", features = ["serde"] }
comrak = "0.30"
serde = { version = "1", features = ["derive"] }
thiserror = "2"
